version: '3.8'

services:
  test:
    image: test:1.0.0
    environment:
      NODE_HUB_ADDRESS: "grid_hub"
    deploy:
      placement:
        constraints:
          - "node.role==manager"
    networks:
      - grid_grid-net

networks:
  grid_grid-net:
    driver: overlay
    external: true
